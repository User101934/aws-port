# Sakthiram V — Portfolio

Static portfolio site.

## Run locally (without Docker)
Open `index.html` in your browser.

## Docker (build & run)
docker build -t sakthiram-portfolio:latest .
docker run -d -p 8080:80 --name portfolio sakthiram-portfolio
Visit http://localhost:8080
